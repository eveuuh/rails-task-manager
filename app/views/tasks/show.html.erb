<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= link_to 'Edit', edit_path(@task) %>

<p><%= link_to "Delete", delete_path,
      method: :delete,
      data: { confirm: "Are you sure?" } %></p>

<h2><%= link_to "back to list", tasks_path %></h2>

